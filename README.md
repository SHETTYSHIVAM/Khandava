# Forest Fire Prediction
## Overview

This project predicts forest fire occurrences in Uttarakhand, India, using meteorological, terrain, and land-use data.
It uses machine learning models (XGBoost, Random Forest) currently for prediction.

**Note:** data/raw/ and data/processed/ are ignored by Git because they can contain very large CSVs.